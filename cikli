#include <iostream>
using namespace std;
//int main() {

	/*int i = 0;
	string username = "anna", u1;


		do {
			i++;
			cin >> u1;
			if (u1 == username) { exit(0); }
		} while (i < 5);
		cout << "out of attempts";*/


	/*int i = 0;
	do {
		i++;
		cout << i << " " << i * i << endl;


	} while (i < 10000000);*/

	/*string myname;
	do {
		cout << "kira" << endl;
		cin >> myname;
		if (myname == "no") { cout << "yes!"; };
		if (myname == "lyakh") { cout << "no it is your surname"; };
	} while (myname != "kira");
	return 0;*/
	bool isNumber(string s)
	{
		for (int i = 0; i < s.length(); i++)
			if (isdigit(s[i]) == false)
				return false;
		return true;

	}
	int main () {
		/*string myname;

		do {
			cout << "kira" << endl;
			cin >> myname;
			if (isNumber(myname)) { cout << "That is a number!"; }
		} while (myname != "kira");
	*/
		string myname; string parole;
	do {

		cin >> myname;
		cin >> parole; 
		if (myname == "no") { cout << "yes!"; };
		
	} while (myname != "kira"|| parole = "907");

	return 0;
}
